---
description: Prisma and data model
globs: "apps/api/prisma/**/*"
alwaysApply: false
---

# Prisma

- Schema changes → named migration (`prisma migrate dev`).
- Index filters by `agencyId` + stage/assignee.
- Seeds are demo-only; never commit real customer PII.
